import 'package:flutter/widgets.dart';

class PinnedMessagesSideBar extends StatelessWidget {
  const PinnedMessagesSideBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
